export const isAuthSelector = state => state.user.isAuth;
export const errorSelector = state => state.user.errorMessage;
export const loadingSelector = state => state.user.loading;
export const isInitializedSelector = state => state.user.isInitialized;
export const nameSelector = state => state.user.name;
